language: python
python:
  - 3.6
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
install:
  - pip install .
script:
  - pytest
after_success:
  - bash <(curl -s https://codecov.io/bash)